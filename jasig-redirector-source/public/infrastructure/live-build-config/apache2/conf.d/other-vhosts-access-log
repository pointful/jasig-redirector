# Define an access log for VirtualHosts that don't define their own logfile
CustomLog "|/usr/bin/cronolog -l ${APACHE_LOG_DIR}/other_vhosts_access.log  ${APACHE_LOG_DIR}/%Y/%m/%Y-%m-%d-other_vhosts_access.log" vhost_combined
